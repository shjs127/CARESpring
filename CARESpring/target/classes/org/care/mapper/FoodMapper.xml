<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.care.mapper.FoodMapper">


	<select id="selectStore"
		resultType="org.care.domain.StoreInfo">
		SELECT *
		from STOREINFO
		where STORENO = 1
	</select>

	<select id="storeLogin" resultType="org.care.domain.StoreInfo">
		SELECT *
		FROM STOREINFO
		 WHERE MANAGENO = #{manageNo} and STORENO = #{storeNo}
	</select>
	
	<select id="selectDetail"
		resultType="org.care.domain.DetailInfo">
		SELECT *
		from DETAILINFO
		where STORENO = 1
	</select>
	
	
	<select id="selectReview"
		resultType="org.care.domain.ReviewInfo">
		SELECT *
		from REVIEWINFO
		where STORENO = 1
	</select>
	
	<select id="selectMenu"
		resultType="org.care.domain.MenuInfo">
		SELECT *
		from MENUINFO
		where STORENO = 1
	</select>
	
</mapper>


